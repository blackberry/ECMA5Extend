require(['../src/extend!../demos/properties/src/SomeType'], function(type) {

	describe('Basics', function() {

		var parentObject;
		var childObject;
		
		beforeEach(function() {
			parentObject = ParentClass.create();
    		childObject = ChildClass.create();
		});

	});

	
});
